apiVersion: apps/v1
kind: Deployment
metadata:
  name: questiondb-deployment
  labels:
    app: questiondb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: questiondb
  template:
    metadata:
      labels:
        app: questiondb
    spec:
      containers:
      - name: mongodb
        image: mongo:5.0
        ports:
        - containerPort: 27017
        env:
        - name: MONGO_INITDB_ROOT_USERNAME
          valueFrom:
            secretKeyRef:
              name: questiondb-secret
              key: mongo-user
        - name: MONGO_INITDB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: questiondb-secret
              key: mongo-password
---
apiVersion: v1
kind: Service
metadata:
  name: questiondb-service
spec:
  selector:
    app: questiondb
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: questiondb-config
data:
  mongo-url: questiondb-service
---
apiVersion: v1
kind: Secret
metadata:
  name: questiondb-secret
type: Opaque
data:
  mongo-user: bW9uZ291c2Vy
  mongo-password: bW9uZ29wYXNzd29yZA==